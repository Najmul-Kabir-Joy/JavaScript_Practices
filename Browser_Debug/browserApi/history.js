/*window.history.go(value);
if value = 0; or blank; refresh the page
if value = positive something; forward the page
if value = negative something; backward the page
if window.history.length = how many history is there. if 1 that means its for the current
*/
